# Applicant's Pre-work

An interactive pre-work page for the UPLB Data Science Guild applicants. Participants open the link days before the session, walk the seven-stage life cycle map, pass a sequence check and a three-question self-check, and write one question to bring.

Static HTML, CSS, and JavaScript. No build step, no dependencies, no network calls. All progress stays in the visitor's browser (`localStorage`).
